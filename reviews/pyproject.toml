[project]
name = "reviews"
version = "0.1.0"
description = "Async journal review system with Claude"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.48",
    "watchfiles>=1.1.1",
]
